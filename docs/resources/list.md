---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "klaviyo_list Resource - klaviyo"
subcategory: ""
description: |-
  A Klaviyo audience list (https://developers.klaviyo.com/en/reference/lists).
---

# klaviyo_list (Resource)

A Klaviyo audience list (https://developers.klaviyo.com/en/reference/lists).

## Example Usage

```terraform
terraform {
  required_providers {
    klaviyo = {
      source  = "aroma-zone/klaviyo"
      version = "~> 0.1"
    }
  }
}

provider "klaviyo" {
  # api_key is read from KLAVIYO_API_KEY if not set here.
  # api_revision defaults to the revision this provider was built for.
}

resource "klaviyo_list" "newsletter" {
  name           = "Newsletter"
  opt_in_process = "single_opt_in"
}

output "newsletter_id" {
  value = klaviyo_list.newsletter.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Human-readable name for the list.

### Optional

- `opt_in_process` (String) Opt-in process for new subscribers. One of `double_opt_in` or `single_opt_in`. Defaults to the Klaviyo account default if omitted.

### Read-Only

- `created` (String) ISO-8601 timestamp the list was created (server-assigned).
- `id` (String) Klaviyo-assigned identifier for the list.
- `updated` (String) ISO-8601 timestamp the list was last updated (server-assigned).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/usr/bin/env bash
# Import an existing Klaviyo list into Terraform state.
# Replace <list-id> with the Klaviyo list ID (e.g. "Y6nRLr").
terraform import klaviyo_list.newsletter <list-id>
```
